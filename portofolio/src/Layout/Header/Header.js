import NavBar from "../../Components/NavBar/NavBar";
import "./Header.css";

const Header = (props) => {
  return (
    <header className="Header">
      <NavBar />
    </header>
  );
};

export default Header;
